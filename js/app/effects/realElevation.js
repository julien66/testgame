/**
 * @file
 * Real elevation module for the game.
 */
define([], function() {
  var tiff;
  
  var call = function() {
    // Request tiff image of the real zone.
  }

  var update = function() {
    // Update terrain man !
  }

  return {
    'call' : call,
    'upadate' : update,
  }
}
